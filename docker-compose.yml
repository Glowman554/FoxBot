version: "3.3"
services:
  foxbot:
    build: .
    volumes:
      - "./srv:/srv"
      - "/etc/letsencrypt:/etc/letsencrypt"
    networks:
      - "router_default"
      - "default"
    privileged: true
    restart: always

networks:
  router_default:
    external: true